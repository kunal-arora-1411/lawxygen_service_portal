# Copy to .env.local.
#
# The API's origin. Public by necessity: the browser calls it directly, because the
# session is an httpOnly cookie the API sets on a sibling host.
NEXT_PUBLIC_API_ORIGIN=http://localhost:4000
